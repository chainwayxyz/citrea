use bitcoin_da::service::BitcoinService;

/// Extracts the batch proofs from the BitcoinService.
pub fn extract_batch_proofs_from_bitcoin(da_service: BitcoinService) -> Vec<u8> {
    return Vec::new();
}
